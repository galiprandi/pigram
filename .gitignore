# Dependencies
node_modules/

# Build output (dist/ is committed so pi install git: works without a build step)
# dist/

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/

# pi runtime (local secrets/cache for this project's own dogfooding)
.pi/pigram.json
.pi/tmp/
